% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unique.R
\name{unique}
\alias{unique}
\alias{unique.eemlist}
\alias{unique.abslist}
\title{Get unique EEMs or absorbance samples}
\usage{
\method{unique}{eemlist}(x, ...)

\method{unique}{abslist}(x, ...)
}
\arguments{
\item{x}{An \code{eemlist} or \code{abslist} object.}

\item{...}{Additional arguments passed to \code{\link[base:unique]{base::unique()}}.}
}
\value{
An object of class \code{eemlist} or \code{abslist} with duplicate samples removed.
}
\description{
Removes duplicate EEM matrices or absorbance data from an \code{eemlist} or \code{abslist}.
Duplicates are determined based on the actual data (\code{eem$x} or \code{abs$data}) rather
than metadata or sample names. Only truly duplicate EEM or absorbance data are removed.
}
\details{
The first duplicate will be retained. The sample name will not be updated but
will reflect the give name of the first duplicated sample.
}
\examples{
# Remove duplicate EEMs
unique_eems <- unique(example_eems)

# Remove duplicate absorbance data
unique_abs <- unique(example_abs)
}
